interface:
  display_name: "PM Resume Builder"
  short_description: "Tailor one-page Chinese PM resumes"
  default_prompt: "Use $pm-resume-builder to create or tailor a one-page Chinese product manager resume from DOCX, PDF, or raw text."
